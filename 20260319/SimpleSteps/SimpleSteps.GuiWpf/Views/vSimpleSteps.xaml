<Window x:Class="SimpleSteps.GuiWpf.Views.vSimpleSteps"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SimpleSteps.GuiWpf.Views"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="35"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0" x:Name="menMain">
            <MenuItem Header="Datei" />
            <MenuItem Header="Bearbeiten"/>
            <MenuItem Header="Hilfe"/>
        </Menu>


        <TabControl Grid.Row="1" x:Name="tabMain">
            <TabItem Header="Dashboard" x:Name="taiDashboard">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50*"/>
                        <RowDefinition Height="50*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="50*"/>
                        <ColumnDefinition Width="50*"/>
                    </Grid.ColumnDefinitions>

                    <!--<Label Grid.Row="0" Grid.Column="0" Content="Raumklima"  />-->
                    <ContentControl Grid.Row="0" Grid.Column="0" 
                                    Grid.RowSpan="2" Grid.ColumnSpan="2" 
                                    Content="{Binding PlotControl}" />
                    <!--<Label Grid.Row="0" Grid.Column="1" Content="Trinkmenge"  />
                    <Label Grid.Row="1" Grid.Column="0" Content="Chart Raumklima"  />
                    <Label Grid.Row="1" Grid.Column="1" Content="Chart Trinkmenge"  />-->
                </Grid>
            </TabItem>

            <!-- Register Messdaten -->
            <TabItem Header="Daten" x:Name="taiData">
                <DockPanel LastChildFill="True">
                    <ToolBar DockPanel.Dock="Top">
                        <Button Content="Aktualisieren" Command="{Binding UpdateCommand}"/>
                    </ToolBar>
                    <!--<DataGrid x:Name="dgMeasuredData" ItemsSource="{Binding MeasuredData}" />-->
                    <DataGrid x:Name="dgMeasuredData" ItemsSource="{Binding MeasuredData}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              CanUserSortColumns="True"
                              CanUserResizeColumns="true"
                              >
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID"
                                                    Binding="{Binding Id}"
                                                    Width="70"
                                                    Visibility="Collapsed"/>
                            <DataGridTextColumn Header="Zeipunkt"
                                                    Binding="{Binding MeasuredDateTime, StringFormat={}{0:dd.MM.yyyy HH:mm}}"
                                                    Width="100"
                                                    />
                            <!--<DataGridTextColumn Header="Messwert"
                                                    Binding="{Binding MeasuredValue, StringFormat={}{0:N2}}"
                                                    Width="100"
                                                    />-->
                            <DataGridTextColumn Header="Messwert"
                                                    Binding="{Binding MeasuredValue, StringFormat={}{0:N2}}"
                                                    Width="100"
                                                    >
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextAlignment" Value="Right" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="Einheit"
                                                    Binding="{Binding Unit}"
                                                    Width="100"
                                                    />

                        </DataGrid.Columns>

                    </DataGrid>
                </DockPanel>
            </TabItem>


            <!-- Register Klienten -->
            <TabItem Header="Klienten" x:Name="taiPersons">
                <DockPanel LastChildFill="True">

                    <ToolBar DockPanel.Dock="Top">
                        <Button Content="Löschen" Command="{Binding DeleteAppUserCommand}"/>
                        <Button Content="Neu" Command="{Binding NewAppUserCommand}" />
                        <Button Content="Speichern" Command="{Binding UpdateAppUserCommand}"/>
                    </ToolBar>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition MaxWidth="150" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Margin="2">
                            <TextBox x:Name="txbSearchValue" 
                                     MinWidth="115"
                                     MaxWidth="115"
                                     Height="27"
                                     Text="{Binding SearchValue, UpdateSourceTrigger=PropertyChanged, Delay=500}" />
                            <Button Width="27" Height="27" x:Name="btnSearchClear" Content="x" Command="{Binding ClearSearchAppUserCommand}" />
                        </StackPanel>
                        <ListBox Grid.Row="1" Grid.Column="0" x:Name="lstAppUsers" 
                                 ItemsSource="{Binding AppUsers}"
                                 DisplayMemberPath="Displayname"
                                 SelectedValuePath="Id"
                                 SelectedValue="{Binding SelectedAppUser.Id}"
                                 SelectedItem="{Binding SelectedAppUser}"
                                 />


                        
                        <TextBlock Grid.Row="2" Grid.Column="0" Text="{Binding AppUsers.Count, StringFormat={}{0} Benutzer}" />

                        <Grid Grid.Row="1" Grid.Column="1" Margin="5">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="35" />
                                <RowDefinition Height="35" />
                                <RowDefinition Height="35" />
                                <RowDefinition Height="35" />
                                <RowDefinition Height="35" />
                                <RowDefinition Height="35" />
                                <RowDefinition Height="35" />
                                <RowDefinition Height="35" />
                                <RowDefinition Height="35" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="25*" MinWidth="100" MaxWidth="130" />
                                <ColumnDefinition Width="75*" />
                            </Grid.ColumnDefinitions>
                            <Label Grid.Row="0" Grid.Column="0" Content="Name" />
                            <Label Grid.Row="1" Grid.Column="0" Content="Vorname" />
                            <Label Grid.Row="2" Grid.Column="0" Content="Geburtsdatum" />
                            <Label Grid.Row="3" Grid.Column="0" Content="Ist aktiv" />
                            <Label Grid.Row="4" Grid.Column="0" Content="E-Mail" />
                            <Label Grid.Row="5" Grid.Column="0" Content="Mobiltelefon" />
                            <Label Grid.Row="6" Grid.Column="0" Content="Geschlecht" />
                            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding SelectedAppUser.Lastname, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />
                            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding SelectedAppUser.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />
                            <!--<TextBox Grid.Row="2" Grid.Column="1" Text="{Binding SelectedAppUser.Birthday}" />-->
                            <DatePicker Grid.Row="2" Grid.Column="1" SelectedDate="{Binding SelectedAppUser.Birthday, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />
                            <!--<TextBox Grid.Row="3" Grid.Column="1" Text="{Binding SelectedAppUser.IsActive}" />-->
                            <CheckBox Grid.Row="3" Grid.Column="1" IsChecked="{Binding SelectedAppUser.IsActive, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding SelectedAppUser.Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />
                            <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding SelectedAppUser.Mobilephone, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                            <!--<TextBox Grid.Row="6" Grid.Column="1" Text="{Binding SelectedAppUser.Sex}" />-->
                            <ComboBox Grid.Row="6" Grid.Column="1"
                                      ItemsSource="{Binding Gender}"
                                      SelectedItem="{Binding SelectedAppUser.Sex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      />

                            <StackPanel Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Content="Löschen" Command="{Binding DeleteAppUserCommand}"/>
                                <Button Content="Neu" Command="{Binding NewAppUserCommand}" />
                                <Button Content="Speichern" Command="{Binding UpdateAppUserCommand}"/>
                            </StackPanel>

                        </Grid>


                    </Grid>




                </DockPanel>
            </TabItem>
            <TabItem Header="Vorhersage" x:Name="taiForecast">
                <StackPanel>

                </StackPanel>
            </TabItem>
        </TabControl>



        <StatusBar Grid.Row="2" x:Name="staStatusbar">
            <StackPanel Orientation="Horizontal">
                <Label  Content="Datenstand vom: "/>
                <TextBlock Text="{Binding LastData, StringFormat={}{0:dd.MM.yyyy HH:mm}}" VerticalAlignment="Center" />
                <Label Content="Letzte Aktualisierung: "/>
                <TextBlock Text="{Binding LastForecast, StringFormat={}{0:dd.MM.yyyy HH:mm}}" VerticalAlignment="Center" />
            </StackPanel>
        </StatusBar>

    </Grid>
</Window>
